# UX-канон Extella (правила Эллы, обязательны для ВСЕХ новых интерфейсов)

Ничего из этого не обсуждается на ревью заново — это принятые решения продукта.
Проверяй каждый новый экран/панель/окно по всем пунктам ДО передачи.

## 1. Перекрытия — запрещены
- Никакой плавающий элемент (чип, фаб, тост, панель) не смеет закрывать контролы:
  поля ввода, кнопки, навигацию. Углы делим заранее: правый нижний — чип журнала,
  левый нижний — сервисные кнопки окна («Починить»).
- Открывающаяся панель ОТОДВИГАЕТ контент (margin/padding), а не ложится поверх.
- Плавающее — перетаскиваемо (ручка ⠿), позиция запоминается.

## 2. Управляемость окон
- Всё, что открывается, можно закрыть (✕), свернуть и/или изменить в размере.
- Размеры и позиции переживают перезапуск (localStorage).
- Резайз — явной ручкой с курсором col-resize/row-resize, клампы разумные (мин/макс).

## 3. Дизайн-токены (единые для витрины, стола, окон, Конструктора)
- Шрифты: Source Serif 4 (заголовки) · Source Sans 3 (текст/UI) · JetBrains Mono (метки, тех-значения).
- Фон #FAF9F5, акцент #C57E33, петроль; радиусы 4–6 (крупные контейнеры 8);
  границы 1px вместо теней; БЕЗ градиентов.
- БЕЗ КАПСА (метки — строчный моно с разрядкой). БЕЗ эмодзи в хроме — только
  линейные иконки (Iconoir-спрайт); эмодзи допустимы только как аватары агентов.

## 4. Язык
- Русский, обращение на «ты». Запрещены: «Визард», «ассистент», «помощник», «бот»,
  «юзер», «делает сама/без тебя». Персонаж интервью — «Мастер». «Сессия» → «проект».
- Без жаргона: мост/PID/localhost/deferred/аллоулист и т.п. — только человеческие слова.
- Текст кнопки = точное обещание клика. Никаких «Закрыть» рядом с «Отмена».
- Английские строки в RU-интерфейсе запрещены; EN — через словарь/`_L(ru,en)`.

## 5. Честность состояний
- Ошибка всегда говорит, ЧТО ДЕЛАТЬ дальше. Сырые «HTTP Error 404» запрещены.
- «Пусто» ≠ «не смог проверить»: отказ загрузки показывает «⚠ Не смог проверить —
  Проверить ещё раз» с работающим повтором.
- «Идёт в фоне» разрешено говорить только при таймауте.
- Кнопка/карточка, за которой ничего нет (нет эксперта, нет экрана), не показывается
  или честно приглушена с тостом-причиной. Ссылок на несуществующие разделы — ноль.
- Фича с внешним компонентом обязана честно деградировать (приглушение + причина + путь).

## 6. Модалки
- Одна модалка — один смысл кнопок: primary = действие, secondary = «Отмена».
- Общие элементы модалок сбрасываются при каждом открытии И закрытии (подписи кнопок не «залипают»).
- Инфо-модалка имеет ОДНУ кнопку «Закрыть».
- Гейты (выбор агента и т.п.) — при первом ДЕЙСТВИИ, не при входе на экран.

## 7. Никаких личных данных в коде
- Личные ID агентов/пути/токены — только динамическое разрешение; известный ID — лишь фолбэк.

Отступление от канона — только по прямому решению Эллы, зафиксированному письменно.
