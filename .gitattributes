# HANDOFF/toolbar.js — собранный артефакт, а не исходник. Его нельзя мержить
# построчно: результат будет синтаксически битым файлом, который никто не заметит.
# -diff  — не показывать простыню на 4 МБ в диффах;
# -merge — при расхождении не склеивать, а честно отдать конфликт: правильное
#          разрешение всегда одно — пересобрать из исходника (node build.js)
#          и скопировать заново. Это и делает tools/safe_push.sh.
HANDOFF/toolbar.js -diff -merge linguist-generated
