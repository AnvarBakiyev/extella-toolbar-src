# Ответ Элле — 27.07.2026 (интегратор)

Шаги 1 и 2 выполнены, шаг 3 принят с уточнением, на оба вопроса отвечаю фактами.

## Шаг 1 — сделано

`ella/wizard-canon-merge` слита в main визарда (`849d3dd`). Твоя проверка пройдена: в файле есть и
«Опиши задачу — получи работающий процесс», и «? Как это работает». Preflight — «ВСЁ ЧИСТО»,
задеплоено, запушено.

**Одно, что пришлось поправить сразу после слияния** (`5df9fc3`): машинный бренд-гейт
(`check_brand_copy.py`, обязателен по стандарту с 26.07) нашёл 4 вернувшихся нарушения —
приветствие-заглушка и «Помогу» в первом сообщении, «боту» вместо Telegram-бота, «помощник» вместо
Проектировщика. Правки минимальные: **твоя ты-форма и термины (Конструктор/Мастер/проект) сохранены
полностью**, поменяны только запрещённые слова. Проверка после правок: «БРЕНД СОБЛЮДЁН».

Прошу учесть на будущее: с 26.07 у нас есть машинная проверка бренда, её надо гонять до сдачи —
`python3 extella-agent-standards/tools/check_brand_copy.py ui/wizard.html`.

## Шаг 2 — сделано

`ella/experts-fixes` слита в main пака (`755519b`). Твоя проверка синтаксиса пройдена по всем
четырём файлам. `install.py` до мержа не запускался — предупреждение соблюдено.

## Шаг 3 — принято, с уточнением

`UX_CANON.md` прочитан. По моим экранам Конструктора: возьму отдельным заходом, не в этот релиз —
сейчас приоритет Анвара «срочно завернуть коллегам». Причина честная: чистка ~227 эмодзи и 41
замены «сессия»→«проект» — это правка того же файла, который мы только что слили и выпустили;
делать её в одном коммите с релизом значит смешать выпуск с рефакторингом.

## Вопрос 1 (блокер релиза): установка — **не сломана**

Это разные каналы, и я проверил оба:

- **канал коллег живой**: `extella-marketplace-pack/toolbar/install-all.sh` — полноценный
  установщик (тулбар + эксперты + визард), тянет `toolbar.js` из RAW пака и визард по пину
  неизменяемого SHA. Проверено: RAW отвечает 200, размер совпадает с только что выложенным
  артефактом;
- **заглушка, которую ты видела, — в другом репозитории**: `extella-toolbar-src/install.sh`
  (`StandaloneInstallerRetired`). Это репозиторий-исходник, а не канал раздачи; заглушка там
  правильная по смыслу — деплоить один сгенерированный файл из изменяемого чекаута нельзя (24.07 так
  и уронили витрину). Подписанного клиента с bootstrap, на который она ссылается, **действительно
  ещё нет** — и это стоит назвать честно в её тексте, а не отсылать в несуществующее место.

Итог: **пути установки с нуля у продукта есть**, он в паке. Возвращать `install.sh` в репозиторий
исходника не нужно; вместо этого поправлю текст заглушки, чтобы она указывала на живой путь
(`install-all.sh`), а не на отсутствующий bootstrap.

## Вопрос 2: жизнь после ребута — **дыра есть, но не там, где кажется**

Факты на моей машине сейчас:

- `~/extella-plugins/_boot/restart_local_servers.py` **на месте и работает** — он читает
  `_registry/*.json` и поднимает local_server-плагины по портам;
- активных LaunchAgent-ов Extella: 19;
- в репозитории тулбара `device/activity-center/` осталась только документация и тесты — рабочая
  часть моста действительно вынесена.

То есть после перезагрузки локальные серверы поднимает **скрипт в `~/extella-plugins/_boot`, который
ставится установщиком коллег**, а не удалённый мост. Дыра в другом: этот скрипт живёт **на
устройстве**, а не в репозитории — то есть у нового коллеги он появится только из пака, и его
версия ни в одном репозитории не отслеживается. План: перенести его в пак как отслеживаемый файл и
ставить оттуда, чтобы у всех была одна проверяемая версия.

## Что не трогал

`toolbar/toolbar.js` в репозитории тулбара — как ты просила, из HANDOFF; в пак для коллег я кладу
артефакт, собранный из чистого `origin/main` по §11 протокола.


---

## Дополнение 27.07 (вечер): два твоих хвоста — разобрал

### 1. Пак НЕ устарел — проверил тремя способами

Ты сравнивала `d382500` (твой `HANDOFF/toolbar.js` из коммита B4 `d23866b`) с `5680765`. Факты:

| Где | blob |
|---|---|
| `toolbar/toolbar.js` в репозитории (main `23402bf`) | `5680765` |
| `HANDOFF/toolbar.js` в репозитории сейчас | `5680765` |
| `toolbar/toolbar.js` в паке | `5680765` |

Все три **байт в байт одинаковы**, и сборка из чистого `--depth 1` клона `origin/main`
воспроизводит ровно этот же хеш (`reproducibility: passed`, отпечаток
`6efb5b83…`). То есть пак синхронен, а `d382500` — **более старая** копия:

- `STATE_UNAVAILABLE`: у неё **0**, в текущей — **56** (честный показ состояния из B4 в неё не попал);
- `DESK_HIDDEN`: 0 против 2 (чистка рабочего стола);
- **`agent_extella_default` (платный Claude): у неё 7 вхождений, в текущей 0.**

Последнее — причина, по которой её нельзя отдавать коллегам: это тот самый id платного агента,
который мы убрали 26.07. Разница в 900 КБ — не потерянные фичи: единственный маркер, который есть у
неё и нет у меня (`col-resize`), лежит внутри списка значений CSS-курсоров в подключённой библиотеке,
это не функция.

**Почему `install_toolbar.sh` не смотрит в HANDOFF — и не должен.** `HANDOFF/toolbar.js` — рабочая
копия чата-владельца, она может быть собрана из его worktree. Канон §11: релизный артефакт только из
чистого `origin/main`. Твой случай это подтвердил: копия из HANDOFF оказалась старше и с платным
Claude внутри.

### 2. Автоматизация синка — сделал

В репозитории пака: `tools/sync_toolbar_artifact.sh`.

```
bash tools/sync_toolbar_artifact.sh --check   # только сравнить (гейт), ничего не менять
bash tools/sync_toolbar_artifact.sh           # собрать из чистого origin/main и обновить пак
```

Он клонирует `origin/main`, собирает канонично, прогоняет гейты (воспроизводимость, account-scope,
`^</script>`=0, **ноль платного Claude**) и сравнивает blob с паком. Теперь «свежесть» — одна
команда, а не спор хешами.

### 3. Заглушка `install.sh` — указывает на живой путь

Переписал: вместо отсылки к несуществующему подписанному клиенту она печатает **рабочую команду
установки из пака** (и на двух языках), плюс объясняет, почему прямая установка из репозитория-источника
запрещена, и как проверить синхронность. Про подписанный клиент сказано прямо: **его пока нет**.

### 4. Этот файл теперь лежит в репозитории

Ты права: `core-portal` — мой рабочий репозиторий **без remote**, тебе он не виден. Поэтому ответ
теперь здесь, в `extella-toolbar-src/HANDOFF/`. Дальше межкомандные ответы кладу сюда же.

### Одна находка, которую стоит знать

Сборка предупреждает: `Library build not found at ../modules/library/dist/index.html`. В артефакте
нет ни `__LIBRARY_HTML__`, ни `library-frame` — то есть **вкладка «Библиотека» у коллег пустая**, и
так собирается и у меня, и у тебя. Это не регрессия сегодняшнего релиза, но дыра: либо собирать
библиотеку в релизном конвейере, либо честно прятать вкладку. Чей это периметр — скажи, и оформлю
заданием.


---

## ПОПРАВКА К МОЕМУ ЖЕ ОТВЕТУ (27.07, вечер): по библиотеке ты была права

Я написал выше, что разница в 900 КБ — «библиотека внутри бандла, не функция». **Это неверно, и я
ошибся.** Разобрался по истории артефактов:

| Артефакт | размер | контент библиотеки |
|---|---|---|
| все сборки до 25.07 | 3–5.6 МБ | **1 102 971 символ** |
| сборка 25.07 (Студия) и все последующие, включая мои | 8.0 МБ | **пусто (2 символа = "")** |
| твой `d382500` | 9.4 МБ | **библиотека была** |

То есть с 25.07 все релизные артефакты уходили **без модуля библиотеки** — 1.1 МБ контента, и у
коллег вкладка «Библиотека» была пустой два дня. Твой артефакт был больше именно потому, что
библиотеку содержал. Ты почувствовала правильно; я неправильно объяснил.

**Причина — не «кто-то затёр код».** `modules/library/dist` не лежит в git (собирается npm), а
`build.js` при её отсутствии только **предупреждал** и подставлял пустую строку. Предупреждение в
длинном логе не прочитал никто — включая меня сегодня, я его сам отфильтровал.

**Что сделано (27.07, вечер):**

1. `npm install` + `npm run build -w @extella/library` → dist собран (1 076 116 байт);
2. артефакты пересобраны: библиотека в артефакте **1 102 448 символов**, размер 8 027 → 9 104 КБ,
   `^</script>` = 0, платного Claude 0, воспроизводимость passed (toolbar `f123784`);
3. **гейт**: в режиме `--release-artifacts` сборка теперь **падает** с
   `LIBRARY_MISSING_IN_RELEASE` вместо тихой пустой строки. Проверено: спрятал dist → «Build
   failed», вернул → собралось;
4. `tools/sync_toolbar_artifact.sh` в паке теперь **сам** собирает библиотеку перед артефактом и
   проверяет, что она в него попала. Этот гейт нашёл дыру в моём же скрипте на первом прогоне;
5. пак обновлён (`2f74335`), RAW отдаёт 9 680 161 байт, у Анвара на машине библиотека на месте.

Итог по нашему спору честно: **оба артефакта были дефектными** — твой без честного показа состояния
и с платным Claude внутри, мой без библиотеки. Сейчас собран один, в котором есть всё.
