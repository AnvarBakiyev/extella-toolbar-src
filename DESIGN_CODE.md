# Дизайн-код Extella

**Статус: канон с 31.07.2026. Отменяет прежние правила (Source Sans 3, радиус 4–6, моно-капс-метки).**

Это не вдохновляющий документ, а рабочий свод. Правило простое: **если решения нет здесь — его нет.**
Новый экран, панель, модалка или плагин считается готовым, когда проходит по этому файлу целиком.

Документ описывает витрину плагинов, панели приложения, Библиотеку и Конструктор — то есть всё,
что видит человек. Значения даны в токенах; хардкод вместо токена — дефект, а не мелочь.

---

## 1. Цвет

Канва тёплая, не белая. Один холодный акцент размечает систему, один тёплый — действие.
Больше акцентов не бывает: если появляется третий, он вытесняет один из этих двух.

### Светлая тема (основная)

| Роль | Токен | Значение |
|---|---|---|
| Канва страницы | `--bg` | `#FAF9F5` |
| Поверхность карточки, модалки | `--s1` | `#FFFFFF` |
| Вторая поверхность (шапки, вкладки) | `--s2` | `#F5F3EC` |
| Третья, четвёртая | `--s3` / `--s4` | `#EFECE2` / `#E5E1D4` |
| Граница обычная | `--bd` | `#D7E0DC` |
| Граница акцентированная | `--bd2` | `#B7CEC9` |
| Текст основной | `--tx` | `#0A0A0A` |
| Текст второго уровня | `--tx2` | `#2A2A2A` |
| Текст третьего уровня | `--tx3` | `#8C8C8C` |
| **Действие** (кнопки, то что нажимают) | `--act` | `#C57E33` бронза |
| **Система** (активные состояния, метки, знаки доверия) | `--a` = `--petrol` | `#2F6B66` петроль |
| Тёплый бренд (декор, категории) | `--brand` | `#C57E33` |

### Тёмная тема

`--bg #0A0A0A` · `--s1 #141414` · `--s2 #181818` · `--s3 #1E1E1E` · `--s4 #262626` ·
`--bd rgba(243,238,229,.09)` · `--bd2 rgba(243,238,229,.18)` ·
`--tx #F5F3EE` · `--tx2 #C9C3B8` · `--tx3 #8C8C8C` ·
`--act #D4944A` · `--a` = `--petrol` `#5FA8A0`.

### Правила цвета

- **Бронза — только действие.** Главная кнопка экрана, «Установить», «Добавить». Не заголовок,
  не рамка, не фон блока.
- **Петроль — только система.** Активная вкладка и чип, метка раздела, подпись «Проверено Extella».
- **Цвет категории** живёт полоской сверху карточки и меткой типа — и больше нигде. Кнопка внутри
  карточки цвет категории не берёт: иначе «Установить» в одном ряду разноцветная.
- Контраст текста ≥ 4.5:1, крупного ≥ 3:1. Чистый чёрный `#000` не используем.
- Градиентов нет. Теней нет — глубину даёт граница 1px.

---

## 2. Шрифты

| Роль | Гарнитура | Где именно |
|---|---|---|
| Интерфейс: всё, что читают и нажимают | **Nunito** 400–700 | лиды, карточки, кнопки, чипы, вкладки, поля |
| Заголовки | **Source Serif 4** | H1 раздела, заголовки модалок |
| Служебное | **JetBrains Mono** | метки разделов, тип объекта в карточке, машинный текст: `agent_`, хэш сборки, команда терминала |

Правило, по которому решается спор: **моно — о предмете (метаданные), Nunito — сам предмет и действие.**
«Обработать документы» — человеческая речь, Nunito. «MCP», «Готовая база», «Работа и карьера» —
факт о предмете, моно.

### Доставка начертаний — обязательная часть

Токен просит семейство, но само начертание должно физически быть в документе. У нас три отдельных
документа, и в каждый шрифт попадает своим путём:

1. **Витрина** носит `@font-face` c data: URI внутри `plugins_manager.html` (кириллица + латиница).
2. **Шелл приложения** вынимает тот же блок из строки витрины (`_etbFontFaceCss()` в `router.js`).
3. **Библиотека и Конструктор** — блок вкладывается в `<head>` при сборке (`build.js`) и в живой файл визарда.

Если добавляешь четвёртую поверхность — она обязана получить начертания одним из этих способов.
Симптом ошибки: интерфейс выглядит «чужим», заголовки уезжают в Georgia, текст в системный гротеск.

---

## 3. Типографика

**Шкала кегля — шесть ступеней, других нет:**

| px | Роль |
|---|---|
| 11 | метки разделов, тип объекта, подписи доверия. Абсолютный минимум |
| 13 | описания карточек, чипы, кнопки, вторичный текст |
| 15 | основной текст, лиды, названия карточек, вкладки |
| 20 | заголовок блока внутри раздела |
| 26 | H1 раздела |
| 40+ | только рекламные поверхности, в продукте не используется |

**Мельче 11px не бывает.** Apple держит минимум 10pt; 8–9px — это не стиль, а недосмотр.

**Веса — три:** 400 текст · 500 подписи и метки · 600 кнопки, заголовки, названия карточек.
700 — единственное исключение: **название карточки в сетке**, потому что при сканировании полки
у него нет других отличий, кроме веса. Кнопке вес не нужен — её метят форма и цвет.

**Интерлиньяж:** текст 1.5–1.6 · заголовки 1.2 · кнопки 1.
**Трекинг:** метки 11px — `.06em`; мелкий текст — `.015em`; H1 — `-.01em`.
**Числа в таблицах и счётчиках** — `font-variant-numeric: tabular-nums`.

⚠️ **Кнопки, поля и списки не наследуют шрифт.** Без правила
`button,input,select,textarea{font-family:inherit}` браузер ставит им Arial. У нас так было
на 82 элементах — весь интерфейс кнопок был набран не тем шрифтом.

---

## 4. Отступы и воздух

**База 4px. Шкала: 4 · 8 · 12 · 16 · 24 · 32 · 48.** Значений вне шкалы не бывает
(5, 6, 7, 9, 10, 11, 13, 14, 18, 22, 26 — запрещены). Линии 1–3px — это хайрлайны, не отступы.

**Правило групп:**

- **связывает** 8–16px: метка ↔ её текст, заголовок ↔ его полка, элементы внутри карточки;
- **разделяет** 24–32px: полка ↔ следующая полка, блок ↔ блок;
- 48px — только между крупными смысловыми частями экрана.

**Разделяет ТОЛЬКО тот, кто начинает новую группу.** В гриде отступы не схлопываются: если низ
предыдущего блока и верх следующего оба задают разделение, они складываются. Так у нас получались
дыры 80–88px. Блок, который заканчивается, отступ снизу не добавляет.

**Первому элементу отделяться не от чего** — у первой метки раздела `margin-top: 0`.

Ориентиры компонентов: паддинг карточки 20 · сетка карточек 16 · чипы между собой 8 ·
абзацы лида 16 · лид до карточек 16.

---

## 5. Форма

| Элемент | Радиус | Токен |
|---|---|---|
| Карточки, панели, модалки, окна | 12px | `--r` |
| Мелкие контролы: чипы, поля, бейджи, вкладки | 8px | `--r-sm` |
| Кнопки-действия | пилюля | `--r-pill` (999px) |
| Круглые иконки-кнопки | 50% | — |

Границы `1px solid var(--bd)`. **Теней нет** — `--shadow: none`. Радиусов вне этих трёх ролей не бывает.

---

## 6. Иконки

- **Библиотека — [Iconoir](https://iconoir.com), только линейный вариант (regular).** Символы лежат
  в `plugins_manager.html` как `<symbol id="ic-*">`, ставятся хелпером `_ico(cat)`.
- Размер 15–17px в строке, 24px в плитке. Толщина линии одинаковая — иконка не должна быть жирнее текста рядом.
- **Эмодзи в интерфейсе запрещены.** Единственное исключение — аватар агента, где эмодзи выбирает
  сам человек. Метка типа, категория, статус, кнопка, пустое состояние — линейная иконка или ничего.

---

## 7. Тексты

- Обращение на **«ты»**, ровно как в витрине.
- **Кнопка — обещание того, что произойдёт.** «Удалить», а не «Удаление».
- **Ошибка говорит, что делать,** и называет кнопку по имени: «…проверь подключение и нажми «Обновить»».
- **Пустое состояние — не ошибка:** «Пока ничего нет» + что сделать, а не «Список пуст».
- Заголовки без точек, без капса, без восклицательных знаков.
- **Запрещённые слова в интерфейсе.** Их не должно быть в подписях, описаниях и метках:
  псевдоанонимизация · юзер · ассистент · визард · деплой · токен · скоуп · эндпоинт ·
  PID · PORT · localhost · мост · реестр (как «список у пользователя») · воркер.
  Это машинные термины: человеку они не говорят ничего, а место занимают.
  Замены: «Псевдоанонимизация» → **«Скрыть личные данные»**; «PORT 8765 · PID 1063» →
  **«работает» / «остановлена»**; сырая ссылка `localhost:8765/…` → кнопка **«Открыть»**.
  Правило одностороннее: имя, один раз выбранное человеком, машинным термином не заменяется.
- **Русская вёрстка:** однобуквенные предлоги не висят в конце строки (`&nbsp;`), фразы переносятся
  по смыслу.
- **Формы числа в русском — четыре, а не три:** `_one` (1, 21), `_few` (2–4), **`_many` (5–20, 25…)**,
  `_other` (дробные). Без `_many` интерфейс молча уезжает на английский. Если `{{count}}` стоит
  вплотную к слову — формы обязательны; шаблон «устройств: {{count}}» безопасен.

---

## 8. Как проверить, что всё соблюдено

**Меряй, а не смотри.** Обход computed-стилей по всем вкладкам — единственный способ поймать
расхождение, которое глаз пропускает:

```js
// в консоли открытой витрины
const f={},s={},w={},r={};
document.querySelectorAll('#grid *, .hdr *, .tabs *').forEach(e=>{
  if(!e.getBoundingClientRect().height) return;
  const cs=getComputedStyle(e);
  r[cs.borderRadius]=(r[cs.borderRadius]||0)+1;
  if(![...e.childNodes].some(n=>n.nodeType===3&&n.textContent.trim())) return;
  f[cs.fontFamily.split(',')[0]]=(f[cs.fontFamily.split(',')[0]]||0)+1;
  s[cs.fontSize]=(s[cs.fontSize]||0)+1; w[cs.fontWeight]=(w[cs.fontWeight]||0)+1;
});
console.log({f,s,w,r});
```

Норма: **3 семейства** (Nunito массово, Serif на заголовках, моно на метках), **не больше 7 кеглей**,
**3–4 веса**, **радиусы только 12 / 8 / 999 / 50%**.

**Машинные гейты (в `tools/safe_push.sh`):**

- `scripts/check-ru-plurals.js` — полнота форм числа; спрашивает категории у `Intl.PluralRules`, а не у списка суффиксов;
- `scripts/check-ui-hardcoded-text.js` — английский текст мимо словаря.

**Правило проверки, выведенное на своих же ошибках:** перевод считается сделанным, когда **экран
показал его глазами**, а не когда строка нашлась в файле. То же и для дизайна: токен объявлен ≠ токен применён.

---

## 9. Антипаттерны — это всё случалось у нас

| Симптом | Причина | Как не повторить |
|---|---|---|
| «Другой шрифт» на кнопках | кнопки не наследуют `font-family` | правило `button,input,select,textarea{font:inherit}` |
| Дыры 80–90px между блоками | разделение задано с обеих сторон стыка, отступы сложились | разделяет только тот, кто начинает группу |
| «Липнет» | связывает 10px, разделяет 14px — веса одинаковые | контраст 2–3× между «связывает» и «разделяет» |
| Английский посреди русского экрана | нет формы `_many` | четыре формы числа |
| Подпись читается только вплотную к экрану | кегль 8–9px | минимум 11px |
| Заголовки в Georgia в панели приложения | токен есть, начертания в документе нет | доставка `@font-face` на каждую поверхность |
| Кнопка «Установить» разного цвета в одном ряду | кнопка берёт цвет категории | цвет действия один, категория — полоска сверху |
| Интерфейс выглядит «детским» | эмодзи в метках и статусах | Iconoir, эмодзи только у аватаров агентов |

---

## 10. Источники, на которых стоит этот канон

- Разбор рефов Эллы (refero.design): **Passionfroot**, **Tuple**, **replit**, **mymind** — общее у всех:
  тёплая не-белая канва, один акцент, плоские карточки с 1px границей, крупное тело текста, воздух между группами.
- **SaaS UI Trends 2026** — «спокойный интерфейс»: типографика вместо иконок и рамок, одно главное
  действие на экран, сложное за раскрытием.
- **Apple HIG (macOS)** — минимизировать число гарнитур, избегать лёгких начертаний, минимум 10pt,
  комфортная плотность без напряжения.
- Практика типографики: ≤2 семейства (+моно ролью), 6–8 кеглей, 3–4 веса, шаг вертикального ритма
  = половина интерлиньяжа.
