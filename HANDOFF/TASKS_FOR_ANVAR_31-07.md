# Анвару — что осталось, 31.07.2026

Порядок доставки принят: правка живёт в репозитории. Где у меня есть доступ — кладу коммитом,
где нет (Конструктор) — присылаю описанием.

Витрина сегодня получила три коммита: `8b768fa`, `5a2cac7`, `4f01083` в `extella-toolbar-src`, ветка `main`.

---

## Статус на вечер 31.07 — после письма Анвара

Одиннадцать панелей приведены к дизайн-коду замером, зафиксированы в семи репозиториях,
Конструктор и «Подключения» — тоже. Пак маркетплейса опубликован.

Что это меняет в списке ниже: пункт 1 закрыт для «Подключений» и пака, остальные продукты
раздаются своими установщиками — открыт. Пункт 7 (тон в Конструкторе) **не** закрыт: замер
проверял шрифты, кегли, радиусы и доставку начертаний, а не слова на экране.

Два новых пункта — 8 и 9.

---

## 1. Пересобрать и раздать пак с текущей витриной

**Почему.** Всё, что ниже помечено «сделано у нас», лежит в `main` и до людей не доедет,
пока раздаётся старый архив. Ровно тот случай, который ловит твой `check_dist_freshness.sh`.

**Что сделать.** Пересобрать пак с `4f01083` (или свежее), прогнать гейт свежести, перевести раздачу.

**Как проверю.** Открываю «Инструменты» → карточка «Деловые письма» говорит
«Напишет деловое письмо: вежливо, коротко и по делу». Если там старое «без „доброго времени суток“» —
раздача отстала.

---

## 2. Эмодзи в системной полосе окна плагина

**Состояние.** Дубль имени ты убрал — спасибо. Осталось эмодзи: оно приходит из данных плагина,
не из шаблона окна, поэтому из тулбара его не срезать, не ломая аватар агента.

**Что сделать.** Либо убрать эмодзи из имени в источнике (как сделал с хвостом «(мой)»),
либо подтвердить, что это осознанный аватар — тогда я оставлю его только в аватаре и срежу из заголовка.

**Как проверю.** Открываю окно любого плагина: в системной полосе одно имя, без картинки перед ним.

---

## 3. Виснет установка: «Worker hung»

**Состояние.** Воспроизводится на `pinokiofactory/RMBG-2-Studio`. Не чинится с нашей стороны.

**Что сделать.** Разобраться с зависанием воркера. Отдельно от этого — стабильный набор кодов ошибок
установки вместо английской строки: `hung`, `timeout`, `not_found`, `forbidden`, `network`, `no_space`.

**Почему коды.** Я уже перевела эти шесть случаев в человеческий текст («Установка застряла и была
остановлена. Попробуй ещё раз — обычно со второго раза проходит»), но сейчас узнаю их по регулярке
на английский текст. Изменишь формулировку на сервере — перевод молча отвалится.

**Как проверю.** Ставлю заведомо ломающуюся программу: в окне человеческая фраза, слова «Worker» нет.

---

## 4. Тон в данных

Тексты карточек, имён и ошибок приходят из пака, и из тулбара я их не поправлю.
Канон один и тот же, он в `DESIGN_CODE.md`:

- описание карточки — глагол и польза: что программа делает и что человек получит;
- без шуток, метафор и оценок («не стыдно показать», «пока кофе не остыл»);
- без слов разработчика: репозиторий, рецепт, зависимости, окружение, воркер, реестр, PID, порт;
- имена без эмодзи в начале и без служебных хвостов.

**Как проверю.** Ни одна карточка каталога не содержит слов из третьего пункта.

---

## 5. Устаревший контрактный тест

`device/activity-center/tests/test_toolbar_integration.py` требует, чтобы в панели встречалось
слово «PID». Мы убрали его как жаргон — тест красный на честном коде.
Файл в твоей зоне (`extella-marketplace-pack/device/activity-center`).

**Что сделать.** Обновить проверку: панель не должна показывать PID, порт и localhost.

---

## 6. Служба фоновых задач 8799 — решение

Плашка активности больше от неё не зависит: она собирает картину из клиентских данных
(что ставится сейчас, какие автоматизации идут, что упало). Служба, если она есть, добавляет
свои задачи сверху — ничего не ломается ни с ней, ни без неё.

**Что решить.** Ставим её всем или снимаем с планов. От ответа зависит, писать ли под неё дальше.

---

## 7. Конструктор — проход по тону

Файл живёт вне репозитория (`~/extella_wizard/app/wizard.html`), поэтому передаю описанием:
иначе правка снова останется на одной машине.

**Правила** (они же в `DESIGN_CODE.md`): обращение на «ты» везде; слова, понятные человеку,
который первый раз собирает агента; никаких слов разработчика в видимом тексте —
конфиг, оркестратор, репозиторий, токен, аллоулист, ТЗ, Process Pack, endpoint.
Внешние названия остаются как есть (BotFather, SMTP, GreenAPI) — это имена чужих сервисов,
но рядом нужна расшифровка, что это и зачем.

**Строки, с которых начать** — их видит каждый новый человек:

| Сейчас | Надо |
| --- | --- |
| Понимаю запрос — агент собирает конфиг… | Понимаю запрос — агент заполняет настройки… |
| ✓ Конфиг собран агентом | ✓ Настройки готовы |
| Агент понял запрос и заполнил конфиг: | Агент понял запрос и заполнил настройки: |
| Соберём Process Pack (репозиторий + карточка) и опубликуем в раздел «AI Автоматизации» витрины | Соберём готовый процесс — карточку и всё, что за ней стоит, — и опубликуем в разделе «AI Автоматизации» |
| Репозиторий: | Исходники: |
| Оркестратор процесса: | Кто ведёт процесс: |
| Паспорта устройств · паспортов устройств пока нет | Подключённые устройства · устройств пока нет |
| сервер будет отключён (вычищен из аллоулиста устройства) | сервер будет отключён и убран из списка разрешённых на этом устройстве |
| План ещё не собран. Завершите интервью и нажми «Собрать план». | Плана пока нет. Закончи разговор и нажми «Собрать план». |
| Текст ТЗ ещё не собран. Нажми «Обновить ТЗ» — документ соберётся из интервью, плана и решений проекта. | Документ ещё не собран. Нажми «Обновить» — он соберётся из разговора, плана и принятых решений. |
| Вернитесь на шаг «Стройка» и нажми «Построить процесс». | Вернись на шаг «Стройка» и нажми «Построить процесс». |
| Опиши источник словами (напр.: «список товаров с ценами из нашего JSON API») | Опиши источник словами (напр.: «список товаров с ценами с нашего сайта») |
| Ссылка на API/данные (если есть). Ключ/токен — отдельно, если нужен. | Ссылка на данные, если она есть. Ключ доступа — отдельным полем, если он нужен. |
| ПДн заменяются стабильными токенами локально на этом устройстве | Личные данные заменяются постоянными метками прямо на этом устройстве |
| агенты раскрывают токены локально (эксперт wz_anon_reveal) | агенты возвращают настоящие значения на этом же устройстве |

**Отдельно.** По файлу вперемешку «ты» и «вы» («Завершите интервью и нажми…») — это в одном
предложении. Канон один: везде «ты».

**Как проверю.** Прохожу сборку агента от первого экрана до публикации: ни одного слова
из списка выше, обращение везде на «ты».

---

## Сделано у нас сегодня — не переделывать

- Плашка активности переведена на клиентские данные; служба перестала быть условием.
- Диалог установки приложения переписан: без «склонирует репозиторий», «рецепта установки»
  и «зависимостей», с честным «обычно это занимает несколько минут». Техническая суть осталась:
  скачиваем с GitHub, код открытый, работает локально.
- Ошибки установки говорят, что делать.
- Все 21 описание программ переписаны по канону тона; лиды вкладок и словарь EN сведены с экраном.

## 8. Workspace и Второй пилот — правка живёт только на одной машине

**Состояние.** Репозитории локально не клонированы, правка внесена в установленные копии
и исчезнет при первой переустановке. Это ровно та болезнь, которую мы сегодня лечили.

**Что сделать.** Клонировать `extella-workspace-engine`, перенести правку, зафиксировать коммитом.
До этого момента считаем, что панели не исправлены — потому что у любого другого человека они не исправлены.

---

## 9. Гейт свежести — на установщики продуктов, не только на пак

**Почему.** `check_dist_freshness.sh` закрывает раздачу пака. Остальные продукты раздаются
своими установщиками, и у них та же дыра между «поправлено» и «человек видит».
Иначе через неделю мы снова будем искать баг в коде, пока раздаётся архив трёхдневной давности.

**Что сделать.** Распространить проверку на архивы продуктов: версия в раздаче указывает
на последний коммит исходников, публикация останавливается сама.

---

## За мной ничего не остаётся

Всё, что было в очереди, либо сделано и лежит в `main`, либо передано выше.
Как появится пересобранный пак — пройду по экранам и скажу, что доехало.
