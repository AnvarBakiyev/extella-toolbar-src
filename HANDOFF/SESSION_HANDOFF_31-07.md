# Передача в новый чат — состояние на 31.07.2026

## Где всё лежит

- Репозиторий: `~/extella-toolbar-src`, ветка `main`, чисто, запушено (последний коммит `2cc8a1a`).
- Витрина: `toolbar/public/plugins_manager.html` → сборка `cd toolbar && node build.js` → `node --check build/toolbar.js`.
- Превью: конфиг `storefront-4600`, порт 4600, служит `toolbar/build`.
- Установка в приложение: `cp -f toolbar/build/toolbar.js ~/Library/Application\ Support/extella-desktop/toolbar.js` (бэкап рядом). Перезапуск Extella делает Элла.
- Пуш: `bash tools/safe_push.sh` (сам подтягивает чужие правки и гоняет гейты).
- Конструктор — живой файл вне репозитория: `~/extella_wizard/app/wizard.html`, копия `HANDOFF/wizard/wizard.html`, бэкапы `.bak.*` рядом.

## Каноны, по которым теперь работаем

- `DESIGN_CODE.md` — дизайн-код продукта (цвет, шрифты, кегли, отступы, радиусы, иконки, тексты, кнопки).
- `BRAND_SURFACES.md` — сайт, презентации, изображения, движение.
- `HANDOFF/DESIGN_CODE_HANDOFF.md` — инструкция команде.
- Брендбук `~/Documents/extella/Extella_BrandBook_JULY_v2.docx` — обновлены 8.3–8.5, 9 и добавлены 8.6–8.9. Бэкапы: `.docx.bak` и `.bak2.docx` рядом.

Коротко: **Nunito** интерфейс · **Source Serif 4** заголовки · **JetBrains Mono** метки и машинный текст ·
кегли 11/13/15/20/26 · веса 400/500/600 (700 только название карточки) · отступы от базы 4 (связывает 8–16,
разделяет 24–32) · радиусы 12 / 8 / пилюля · **бронза = действие, петроль = система** · иконки Iconoir,
эмодзи только аватар агента · тексты на «ты», без жаргона и вычурности, тире по минимуму.

## Сделано сегодня (не переделывать)

Типографика и ритм по всему тулбару; Inter → Nunito; два акцента с ролями; радиусы;
эмодзи вычищены; экран автоматизаций пересобран таблицей (расписание + последний запуск +
«Запустить» из строки); правый клик на столе разделён на «Удалить ярлык» и «Удалить установку»
с возвратом скрытых; устаревшее убрано с полок, но осталось в поиске с меткой; блок
«Установленное» разделён на «Твои программы» и «Встроенное», мигание вылечено отпечатком;
шаблон окна плагина: текст ошибки, пилюля отправки, радиусы, иконка вместо скрепки;
жаргон (PID/PORT/localhost/реестр) убран из плашки процессов; «Псевдоанонимизация» →
«Скрыть личные данные»; «Картинки» → «Изображения»; вкладка Конструктора → «Сборки устройства».

## Очередь — с чего начинать в новом чате

1. **Плашка активности** (`toolbar/src/panels/activity-center.js`) — переделать на клиентские данные:
   что ставится сейчас (`installingIds`), какие автоматизации идут, был ли упавший запуск
   (`agent_runs`). Сейчас читает только службу `127.0.0.1:8799`, которой нечего показывать,
   и потому всегда «Система спокойна». Решение Эллы — вариант А.
2. **Проход по тону** — Конструктор → лиды шести вкладок → подписи карточек (~120 строк).
   Тон подтверждён на пяти примерах: убрать метафоры, шутки, «красивые пары через тире»;
   писать, что делает и что человек получит.
3. **Хвост «(мой)»** — чистильщик `_dtCleanLbl` применяется только к ярлыкам стола;
   в шапке окна плагина имя выводится сырым. Ждём ответа Анвара: убирать хвост у источника
   или срезать у себя во всех местах показа.
4. **Эмодзи и дубль названия в системной полосе окна плагина** — приходят из данных плагина,
   не из шаблона; чинить там же, где имя.

## Что висит на Анваре

Отчёт: `HANDOFF/REPORT_TO_ANVAR_31-07.md`. Главное: виснет воркер установки («Worker hung»,
воспроизводится на `pinokiofactory/RMBG-2-Studio`); откат правок в `extella-connectors`
(все десять пунктов из его письма 30.07 вернулись); `extella_anon.json` в паке;
хвост «(мой)»; служба фоновых задач 8799.

## Рабочие правила Эллы

Правки — локально и в превью, деплой в приложение по её слову, перезапуск Extella она делает
сама. Пуши по ходу работы — норма. Перед пушем `safe_push.sh` (в репозитории параллельно
работает второй чат). Кредиты не жечь: без нужды не перезапускать Extella и не гонять агентов.
