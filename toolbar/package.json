{
  "name": "@extella/toolbar",
  "version": "6.0.0",
  "private": true,
  "description": "Extella Desktop toolbar (CommonJS build script). Embeds the Library SPA + plugins marketplace into a single injectable toolbar.js.",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  }
}
